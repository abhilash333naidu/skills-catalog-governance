# SESSION HANDOFF — skills-catalog-governance product refinement

**Date:** 2026-08-11
**Handed off by:** Coder CEO (Hermes) — continue from here

---

## WHERE WE ARE (verified, literal state)

**Product:** skills-catalog-governance — a skill that solves "bloated skills catalog" by
discovering → grouping → council-merging similar skills into ONE master → benchmarking →
promoting. Published PUBLIC on GitHub.

- **Repo (dev copy):** C:\Users\abhil\Dev\skill_gov  (git repo, branch `main`, CLEAN)
- **Published:** https://github.com/abhilash333naidu/skills-catalog-governance (PUBLIC, default branch `main`)
- **One-liner installs (live, verified 200):**
  - macOS/Linux: `curl -fsSL https://raw.githubusercontent.com/abhilash333naidu/skills-catalog-governance/main/install.sh | bash`
  - Windows: `irm https://raw.githubusercontent.com/abhilash333naidu/skills-catalog-governance/main/install.ps1 | iex`
- **Project spine:** docs/PROJECT.md (decisions D1-D6, milestones M1-M7, all DONE)

**Milestone status (all verified, literal outputs captured in docs/):**
- M1 discovery (`detect-skills`) — DONE: 211+ skills, 0 errors; defects D1-D6 closed/reclassified
- M2 grouping (`detect-groups`) — DONE + v3.1 FIXED: over-grouping eliminated (24-member
  mega-group → 7 clean groups); strong-pair rule + --max-group-size
- M3 council — DONE (commit family): TWO-survivor verdict; transcript in docs/
- M3.5 golden-output gate — DONE: 6/6 match, absorption authorized
- M4 master build — DONE: staged draft, G0 5/5, covenant clean
- M5 benchmark — DONE: G2 confirmation 36/36 cells PASS (3 runs/cell, 4 conditions)
- M6 promotion — DONE: git init, caveman-commit v2.0.0 live, sha256 match, re-audit PASS
- M7 release — DONE: v3.0 → v3.1 → v3.2 (installer) shipped
- v3.2 installer — DONE: install subcommand + install.sh + install.ps1; harness detection
  (opencode/pi/claude/codex/omp/hermes/master/gstack); interactive picker; idempotent
  (no silent overwrite; --yes + timestamped .bak)
- v3.2.3 — branch renamed master → main; one-liner URLs updated; verified

**Team routing (user-mandated):**
- FreeBuff (herdr pane wV:p3, GPT-5.6 Luna) = the CODER — all hard coding
- opencode (herdr pane wV:p4, Build agent) = the COUNCIL — only for LLM-council
  decisions (best-option/solution judgment)
- Me (Hermes/Coder CEO) = ORCHESTRATOR — verification, gates, promotion surgery,
  loss-checks, evidence — NEVER delegate these

**Known operational quirks (IMPORTANT):**
1. FreeBuff Enter issue: `herdr pane send-text`/`pane run` STAGES text but Enter often
   doesn't fire for multi-line tasks. WORKAROUND: write the task/spec to a file, send a
   SHORT one-line instruction, and ask the USER to press Enter in the FreeBuff pane.
2. opencode permission prompts: when blocked on "Permission required", press Right ONCE
   (Allow always) + Enter. Right twice = Reject (mistake made once).
3. opencode Plan agent vs Build agent: Tab switches. Plan mode won't execute — switch
   to Build and re-submit.
4. Windows python vs MSYS paths: python subprocess spawns WSL bash (not git-bash); for
   bash-in-python tests use /mnt/c/... paths; for git-bash install tests use
   `cygpath -w`-converted paths (install.sh already handles this internally).
5. Python env: `unset PYTHONHOME` before running python/pytest.
6. gh credentials: TWO accounts (abhilash333naidu primary, nicknaidu256-lang secondary).
   Before push: `gh auth switch --user abhilash333naidu && gh auth setup-git` — the
   credential manager silently uses the wrong account otherwise (403).
7. search_files tool was flaky on this repo — use terminal grep when it fails.

---

## THE DECISION: REFINE THE PRODUCT BEFORE THE NEXT GROUP

User decided: do NOT start the next skill family yet. First refine the product itself.
The 7 clean groups are queued (understand-*, caveman-help/ponytail-help, careful/guard,
context-restore/save, qa/qa-only, tdd-iron-law/compact, web-design-guidelines/writing-guidelines)
but ONLY after refinement.

## REFINEMENT BACKLOG (proposed — validate, prioritize, execute)

### P1 — Correctness gaps (hard bugs first) — ALL DONE 2026-08-11, verified
- R1. **install.ps1 end-to-end untested** — only parse-checked (pwsh exists at
  C:\Users\abhil\AppData\Local\Microsoft\WindowsApps\pwsh). Run a real install.ps1
  install into a fake home. HIGH. → **DONE PASS** (local-checkout path + real `irm | iex`
  one-liner, isolated HOME; check_package PASS; sha256 matches repo).
- R2. **One-liner interactive picker untested end-to-end** — install.sh tested with
  --target only; the curl|bash → /dev/tty → picker flow needs a real tty test. HIGH.
  → **DONE — found + fixed REAL BUG (v3.2.4, commit 9333c34)**: the documented one-liner
  died instantly (`BASH_SOURCE[0]: unbound variable` under set -u in pipe mode — only ever
  tested as `bash install.sh --target`). Second defect: /dev/tty reconnect used `-r` (permission
  bit passes but open fails ENXIO in no-tty contexts, killing the script) → fixed with
  `(exec 0</dev/tty) 2>/dev/null` probe. 3 acceptance tests PASS: non-tty pipe install,
  interactive picker via WSL pty (selects only chosen harness), file-mode regression.
- R3. **Multi-harness install (select "all")** — picker unit-tested, CLI path untested. MED.
  → **DONE PASS** (install --yes, 4 fake harnesses, all installed, aggregate + per-harness
  check_package PASS).
- R4. **Promotion rollback untested** — we create .bak backups but never verified
  restore actually works. Test: install --yes, corrupt the install, restore from .bak,
  check-package must PASS. HIGH (it's a safety claim). → **DONE PASS** (corrupt → FAIL
  detected → restore .bak → PASS; restored sha256 == repo).

### P2 — Product-quality gaps (make it trustworthy)
- R5. **M2 grouping heuristic validation** — max_group_size=8 is a guess; strong-pair
  threshold (both signals) validated on one catalog. Run detect-groups on a second
  catalog (e.g. the pi store or a cloned public repo) and check group quality. MED.
  → **DONE VALIDATED 2026-08-11** (tech-leads-club/agent-skills, 88 skills: 7 groups, 0
  oversized; deploy family 4/4; security trio correctly NOT grouped). No code change.
- R6. **Usage-aware discovery unvalidated** — --usage-dir added but never tested against
  real Hermes .usage.json shape. Verify the real file's shape and that enrichment works
  on it. LOW-MED. → **DONE — found + fixed REAL BUG (v3.2.5, commit 09d63f6)**: real shape
  is nested objects ({"skill": {"use_count": N}}), loader only accepted flat ints →
  163-entry file returned 0 counts (silent no-op). Verified 40 tests + real-file check.
- R7. **G2 judge is a single correlated model** — the benchmark judge (me) is the same
  base model family as the generator. Consider a rubric file + multi-model judge or at
  least document the limitation loudly in benchmark.json. MED.
  → **DONE (commit 884c77f)** — references/g2-judge-rubric.md + loud note in SKILL.md M5;
  cross-model judge procedure documented for future runs.
- R8. **Council verdict schema** — council-verdict files are prose; make them
  machine-parseable (YAML frontmatter: verdict, survivors[], recategorizations[],
  absorbed[], gates_passed[]). MED (needed for M3 to be reproducible at scale).
  → **DONE (commit 78c89e3, v3.2.6)** — validate-council-verdict subcommand +
  schemas/council-verdict.schema.json; stdlib list-aware parser; 47 tests pass;
  check-package now requires the schema.

### P3 — Polish
- R9. `oversized_groups` exists but SKILL.md M3 has no explicit "what to do when a group
  is oversized" guidance — document it.
- R10. docs/PROJECT.md needs v3.2 entries (installer milestone) — spine is stale.
- R11. A real .usage.json sample/fixture for tests (currently only synthetic).
- R12. Version-bump discipline: SKILL.md says v3.1.0 but repo is at v3.2.3 — reconcile.

---

## VERIFICATION DISCIPLINE (binding — do not skip)

- Every claim backed by literal command + literal output, inline.
- Never accept a subagent's self-reported pass — re-run tests/gates yourself.
- G0/G1/G3 on any SKILL.md change; check-package PASS before any commit.
- Full suite: `unset PYTHONHOME && python -m pytest tests/ -q` (currently 35 passed).

## NEXT-SESSION START SEQUENCE

1. `cd /c/Users/abhil/Dev/skill_gov && git status && git log --oneline -5` — confirm clean main.
2. Re-list herdr panes (`herdr pane list`) — pane IDs may have changed; find FreeBuff
   and opencode panes fresh.
3. Present the refinement backlog (P1 → P3) to the user, get priorities, then execute
   via: FreeBuff for coding, opencode for council judgment, me for verification.
4. Update docs/PROJECT.md as each refinement lands. Commit + push per change.
